#include "main.h"

/**
* _islower -> checks if the character is lowercase
* @c: character argument
* Return: returs 1 and 0 depending on work
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
